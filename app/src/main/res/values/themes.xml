<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ExpertSubmission" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/green</item>
        <item name="android:screenOrientation">portrait</item>
    </style>

    <style name="Theme.ExpertSubmission" parent="Base.Theme.ExpertSubmission" />

</resources>